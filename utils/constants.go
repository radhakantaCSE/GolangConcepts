package utils

// Exposed to outside of package
var Expose = "I am exposed to out side"

// Only accessible inside same package. Not exposed to outside of package
var unExposed = "I am not visible to out side because of my lower case start char"
